{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@material-ui/core": "^3.7.0",
    "axios": "^0.18.0",
    "node-sass": "^4.11.0",
    "numeral": "^2.0.6",
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-scripts": "2.1.1"
  },
  "scripts": {
    "start": "yarn && react-scripts start",
    "build": "react-scripts build",
    "deploy": "yarn && yarn build && netlify deploy --dir ./build --prod && echo Deploy complete."
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
